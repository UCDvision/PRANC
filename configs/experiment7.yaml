name: "PRANC Training of CIFAR-10 with Resnet-56 with 6,000 alphas"
id: "R56_CIFAR_PRANC"
gpus: [1]

pranc:
  seed: 0 
  num_alpha: 6000
  window_size: 6000  
  lr: 0.00001

experiment:
  mode: 'train'   #[train, test]
  method: 'pranc'    #[normal, pranc]
  lr: 0.005
  epoch: 201
  batch_size: 256   #optional for testing
  resume: 'R56_CIFAR_PRANC/pranc'  #for resuming training. if pranc, should be the signature path. if normal, should be the model path
  # load_model: 'R20_CIFAR_PRANC/pranc' #for testing 
  task: 'cifar10'
  model_arch: 'resnet56'

dataset:
  image_width: 32 # set 32 for cifar, 64 tiny, 256 for imagenet
  dataset_path: './datasets'

monitor:  #optional for testing
  log_rate: 50
  save_model: 'best_model.pt' 
  save_path: 'pranc'
